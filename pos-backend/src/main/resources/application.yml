spring:
  application:
    name: pos-system

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/cp_test}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Suhas@123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:prajwalrayar363@gmail.com}
    password: ${MAIL_PASSWORD:ezmi lpao mvxf dnvg}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:true}

  devtools:
    livereload:
      enabled: true

server:
  port: 5000

razorpay:
  api:
    key: ${RAZORPAY_API_KEY:}
    secret: ${RAZORPAY_API_SECRET:}



stripe:
  api:
    key: ${STRIPE_API_KEY:}

gemini:
  api:
    key: ${GEMINI_API_KEY:}

app:
  brand-name: ${APP_BRAND_NAME:SmartPos}
  mail:
    from-name: ${MAIL_FROM_NAME:SmartPos}
  frontend:
    reset-url: http://localhost:5173/auth/reset-password?token=

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
